- name: data types in ansible
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
  tasks:
    - name: print the data types in screen
      ansible.builtin.debug:
        msg: "yes the class is libe class"
      when: IS_LIVE
